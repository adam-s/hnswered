node_modules/
# Keep dist/ in git so contributors can clone and load directly without building.
# Source maps are excluded — they're regenerated by `pnpm build` if needed for debugging.
dist/**/*.map
.snapshots/
.perf/
.impersonate/
.chaos/
.DS_Store
*.log
.vscode/
tmp/
