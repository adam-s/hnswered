node_modules/
# Keep dist/ in git so contributors can clone and load directly without building.
# Source maps are excluded — they're regenerated by `pnpm build` if needed for debugging.
dist/**/*.map
.snapshots/
.perf/
.impersonate/
.chaos/
.audit/
.DS_Store
*.log
.vscode/
tmp/
.analysis/
analysis/__pycache__/
*.pyc
.venv
cost-analysis/data/
# Local-only design notes — kept on disk as reference, out of git history.
docs/logic.md
docs/sequences.md
docs/todos.md
